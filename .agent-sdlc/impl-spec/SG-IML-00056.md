---
id: "SG-IML-00056"
kind: implementation-spec
schema_version: 1
title: "Use UnaryOperator for addQuotes"
description: "Plan a controller-local cleanup that resolves Sonar rule java:S4276 by replacing the generic `Function<String, String>` type of `addQuotes` in `V1SearchController` with `UnaryOperator<String>`."
status: draft
backlog_id: "SG-BLG-00047"
priority: p1
owner: "agent:Rook"
created_at: 2026-04-15T15:43:52Z
updated_at: 2026-04-15T15:43:52Z
module_scope:
  - "backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java"
verification_mode: manual
tags:
  - springguard
  - sonarqube
  - controller
  - v1searchcontroller
  - java-s4276
links:
  requirements: []
  principles: []
  backlog: ["SG-BLG-00047"]
---

# V1SearchController Implementation Spec

## Summary

- Problem: `addQuotes` is typed as `Function<String, String>` even though it takes and returns the same type, and Sonar flags that generic interface choice under `java:S4276`.
- Goal: Replace the declaration type with `UnaryOperator<String>` while preserving the exact quoting behavior and keeping the helper local to `V1SearchController`.
- In scope: Update the functional interface type, corresponding import, and the `addQuotes` declaration.
- Out of scope: Changing the quoting algorithm, moving the helper into another class, or refactoring unrelated functional interfaces.

## Related Specs

- `SG-IML-00055`: Replaces the same anonymous inner class with a lambda; both specs should converge on a single `UnaryOperator<String> addQuotes = s -> ...;` declaration.

## Clarifications Required

- `None`

## Existing Code Touchpoints

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`: Imports `java.util.function.Function` and declares `addQuotes` as `Function<String, String>`.
- Existing `createUnionQuery(...)` flow: depends on `addQuotes` returning the same quoted string as today.
> [ ] [Kestrel (Codex: 019d915e-24a7-7ef1-81fe-9a282f24cc41)] [2026-04-15T15:52:15Z]: This touchpoint is source-incorrect. In the current controller, `createUnionQuery(...)` never references `addQuotes`; it assembles its quoted query fragments inline. The real `addQuotes` usage is `joinQuotedValues(...)`, which feeds `addChildrenFilter(...)` and `addAllChildrenFilter(...)` in `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`. Please point the spec at that actual call chain so the interface-tightening change is validated against the code that really depends on this helper.

## Files To Add

- `None`

## Files To Update

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`: Replace the `Function<String, String>` declaration type with `UnaryOperator<String>` and update the corresponding import usage.

## Library And Framework Decisions

- Reuse:
  - JDK `UnaryOperator<String>`: the more specific functional interface for a same-type input/output transformation.
  - Existing helper behavior: keep the exact quoted-string construction logic unchanged.
- New dependency:
  - `None`
- Rejected options:
  - Keeping `Function<String, String>` for familiarity: rejected because the helper is semantically a unary operator and Sonar requires the narrower type.
  - Converting to a named method instead of a functional field: rejected because the backlog item is specifically about interface choice, not declaration shape.

## Functional Requirements

- `FR-1`: Replace `Function<String, String>` with `UnaryOperator<String>` for the `addQuotes` declaration.
- `FR-2`: Update imports so the file no longer depends on `Function` for this helper and uses `UnaryOperator` instead.
- `FR-3`: Preserve the exact quoted output for every input string.
- `FR-4`: Allow the final declaration to be lambda-based if implemented together with the related lambda-conversion backlog item.

## API Contract

### Endpoint: `GET /api/search`

- Purpose: Preserve the existing search endpoint contract while tightening the type specificity of an internal functional helper declaration.
- Method: `GET`
- Path: `/api/search`
- Authentication: Unchanged.
- Query parameters: unchanged.
- Success response:
  - Status code: `200`
  - Content type: `application/json`
  - Response body: unchanged.
- Error responses:
  - Existing endpoint errors remain unchanged.
- Notes: This is an internal declaration cleanup and should not affect endpoint behavior.

## Data Model And DB Schema

### Schema Changes

| Object | Change Type | Details |
| --- | --- | --- |
| `None` | `none` | `No schema, persistence, or migration work is part of this backlog item.` |

### SQL Or Migration Detail

```sql
-- None.
```

### Data Backfill And Rollback

- Backfill steps:
  - `None`
- Rollback expectations:
  - Revert only the interface/import change if it introduces an unexpected typing issue.
- Deployment ordering constraints:
  - `None`

## Phase Plan

### Phase 1: Narrow the functional interface type

- Goal: Make the helper declaration Sonar-compliant with no behavior change.
- Prerequisites:
  - Confirm `addQuotes` accepts and returns `String` and therefore fits `UnaryOperator<String>`.
- Tasks:
  - `P1-T1`: Replace the `Function<String, String>` declaration type with `UnaryOperator<String>`.
  - `P1-T2`: Update imports to remove `Function` if it becomes unused and add `UnaryOperator`.
  - `P1-T3`: If the related lambda-conversion item is implemented at the same time, ensure the final declaration is a single `UnaryOperator<String> addQuotes = s -> ...;` statement.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`
- Acceptance criteria:
  - The declaration uses `UnaryOperator<String>`.
  - The file imports the correct functional interface and has no stale `Function` import for this helper.

### Phase 2: Verify compile safety

- Goal: Confirm the type tightening compiles and preserves behavior.
- Prerequisites:
  - Phase 1 is complete.
- Tasks:
  - `P2-T1`: Review the declaration and imports to confirm the helper still returns the same quoted strings.
  - `P2-T2`: Run `mvn -pl backend -am -DskipTests compile` from the repo root to confirm the backend module still compiles after the change.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`
- Acceptance criteria:
  - The backend module compiles successfully without unit tests.
  - The helper behavior remains unchanged while the interface type is more specific.

## Risks And Operational Notes

- Risk:
  - Updating the declaration type without cleaning up imports and leaving an unused `Function` import behind.
- Mitigation:
  - Treat the import update as part of the in-scope implementation and verify the final declaration compiles cleanly.
- Monitoring or rollout note:
  - No rollout step is expected because the helper behavior is unchanged.

## Completion Criteria

- The listed files are fully implemented.
- The contract and schema changes match the spec.
- Each phase acceptance criterion is satisfied.
- No unresolved ambiguity remains for in-scope work.
