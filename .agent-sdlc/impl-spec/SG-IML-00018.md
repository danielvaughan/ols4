---
id: "SG-IML-00018"
kind: implementation-spec
schema_version: 1
title: "Document intentionally empty handleError in V1PropertyController"
description: "Plan a controller-local change that resolves Sonar rule java:S1186 by documenting why the intentionally blank `handleError` method in V1PropertyController has no executable body."
status: done
backlog_id: "SG-BLG-00008"
priority: p1
owner: "agent:Kestrel"
created_at: 2026-04-15T14:11:33Z
updated_at: 2026-04-15T14:48:22Z
module_scope:
  - "backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1PropertyController.java"
verification_mode: manual
tags:
  - springguard
  - sonarqube
  - controller
  - v1propertycontroller
  - java-s1186
links:
  requirements: []
  principles: []
  backlog: ["SG-BLG-00008"]
---

# V1PropertyController Implementation Spec

## Summary

- Problem: `V1PropertyController.handleError(HttpServletRequest, Exception)` is an empty public method under `@ExceptionHandler(ResourceNotFoundException.class)` and `@ResponseStatus(HttpStatus.NOT_FOUND)`, which triggers Sonar rule `java:S1186`.
- Goal: Add a nested comment that explains the method is intentionally blank because Spring derives the HTTP 404 response from the surrounding annotations.
- In scope: Update only the body of `handleError(...)` in `V1PropertyController` to include a short explanatory comment.
- Out of scope: Adding logging, changing the response status, throwing `UnsupportedOperationException`, or altering any controller endpoint logic.

## Related Specs

- `None`

## Clarifications Required

- `None`

## Existing Code Touchpoints

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1PropertyController.java`: Contains the annotated `handleError(HttpServletRequest, Exception)` method flagged by Sonar.
- `@ExceptionHandler(ResourceNotFoundException.class)` and `@ResponseStatus(HttpStatus.NOT_FOUND)`: These annotations already define the runtime behavior that the new comment must describe.

## Files To Add

- `None`

## Files To Update

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1PropertyController.java`: Add an inline comment inside `handleError(...)` that states the method is intentionally blank because the annotations fully define the 404 response.

## Library And Framework Decisions

- Reuse:
  - Existing Spring MVC exception-handling annotations: they already provide the desired 404 behavior and should remain the only runtime behavior.
  - Inline nested comment: the smallest compliant fix for Sonar `java:S1186` when the empty method is intentional.
- New dependency:
  - `None`: no dependency is required for a comment-only documentation change.
- Rejected options:
  - Throwing `UnsupportedOperationException`: rejected because the exception handler is part of the controller's active 404 flow, not an unsupported code path.
  - Adding logging or side effects: rejected because this backlog item is only about documenting the intentional empty override.
  - Removing the method entirely: rejected because it would change the controller's explicit exception-handler declaration.

## Functional Requirements

- `FR-1`: `handleError(HttpServletRequest req, Exception exception)` shall remain executable-no-op logic-wise and gain one nested comment inside the method body.
- `FR-2`: The comment shall explain that the method is intentionally blank because the paired `@ExceptionHandler` and `@ResponseStatus` annotations already define the HTTP 404 response.
- `FR-3`: No logging, no additional exception throwing, and no request/response mutation shall be introduced.
- `FR-4`: The method signature, annotations, and visibility shall remain unchanged.

## API Contract

### Exception handling boundary

- Purpose: Preserve the controller's current 404 handling while documenting why the `handleError` method intentionally contains no executable statements.
- Method: Internal Spring MVC exception handler.
- Path: All endpoints exposed by `V1PropertyController`.
- Authentication: Unchanged.
- Inputs:
  - `HttpServletRequest req`: remains unused by design.
  - `Exception exception`: remains unused by design.
- Success response:
  - `Not applicable`: this handler is only used on error paths.
- Error responses:
  - `404`:
    - Trigger: `ResourceNotFoundException` is raised by the controller.
    - Response body: unchanged; `@ResponseStatus(HttpStatus.NOT_FOUND)` continues to define the HTTP status.
- Notes: The only permitted implementation change for this backlog item is an inline explanatory comment inside `handleError(...)`.
## Data Model And DB Schema

### Schema Changes

| Object | Change Type | Details |
| --- | --- | --- |
| `None` | `none` | `No persistence, schema, or migration changes are part of this backlog item.` |

### SQL Or Migration Detail

```sql
-- None.
```

### Data Backfill And Rollback

- Backfill steps:
  - `None`
- Rollback expectations:
  - Remove only the explanatory comment if it proves inaccurate.
- Deployment ordering constraints:
  - `None`

## Phase Plan

### Phase 1: Document the intentionally blank exception handler

- Goal: Make the empty method self-explanatory to maintainers and Sonar.
- Prerequisites:
  - Confirm `handleError(...)` is the controller's only `ResourceNotFoundException` handler.
- Tasks:
  - `P1-T1`: Add a one-line nested comment inside `handleError(...)` that explicitly states Spring handles the 404 response through annotations and no extra code is required here.
  - `P1-T2`: Keep the method body otherwise empty and preserve the existing annotations and signature.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1PropertyController.java`
- Acceptance criteria:
  - `handleError(...)` contains an explanatory comment and no executable statements.
  - The method's annotations and signature are unchanged.

### Phase 2: Verify controller behavior is unchanged

- Goal: Ensure the documentation-only change does not alter runtime behavior.
- Prerequisites:
  - Phase 1 is complete.
- Tasks:
  - `P2-T1`: Review the controller to confirm `ResourceNotFoundException` still maps to HTTP 404 exclusively through the existing annotations.
  - `P2-T2`: Run `mvn -pl backend -am -DskipTests compile` from the repo root to confirm the backend module still compiles after the change.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1PropertyController.java`
- Acceptance criteria:
  - The backend module compiles successfully without enabling unit tests.
  - The controller's 404 handling behavior remains unchanged.

## Risks And Operational Notes

- Risk:
  - Adding executable logic to the handler could change how controller errors are rendered or logged.
- Mitigation:
  - Restrict the fix to an explanatory nested comment only.
- Monitoring or rollout note:
  - After implementation, manually trigger one not-found request against `V1PropertyController` and confirm the endpoint still returns HTTP 404.

## Completion Criteria

- The listed files are fully implemented.
- The contract and schema changes match the spec.
- Each phase acceptance criterion is satisfied.
- No unresolved ambiguity remains for in-scope work.
