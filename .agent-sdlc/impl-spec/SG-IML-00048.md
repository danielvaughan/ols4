---
id: "SG-IML-00048"
kind: implementation-spec
schema_version: 1
title: "Remove commented-out highlight block from configureQueryExecution"
description: "Plan a controller-local cleanup that resolves Sonar rule java:S125 by deleting the commented-out highlight configuration block in `V1SearchController.configureQueryExecution(...)`."
status: draft
backlog_id: "SG-BLG-00039"
priority: p1
owner: "agent:Rook"
created_at: 2026-04-15T15:38:18Z
updated_at: 2026-04-15T15:38:18Z
module_scope:
  - "backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java"
verification_mode: manual
tags:
  - springguard
  - sonarqube
  - controller
  - v1searchcontroller
  - java-s125
links:
  requirements: []
  principles: []
  backlog: ["SG-BLG-00039"]
---

# V1SearchController Implementation Spec

## Summary

- Problem: `configureQueryExecution(...)` contains a six-line commented-out highlight block that Sonar flags as dead code under `java:S125`.
- Goal: Remove the commented-out highlight block without changing the active query configuration logic.
- In scope: Delete only the inactive highlight-related comment lines at the flagged location.
- Out of scope: Reintroducing highlighting, changing active facet configuration, or refactoring adjacent comments in the same method.

## Related Specs

- `SG-IML-00049`: Adjacent removal of a different commented-out line in the same method.
- `SG-IML-00050`: Adjacent cleanup of the remaining TODO comment in the same method.

## Clarifications Required

- `None`

## Existing Code Touchpoints

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`: `configureQueryExecution(...)` contains the inactive highlight block directly above active facet-field configuration.
- Active query execution statements immediately before and after the comment block: must remain unchanged.

## Files To Add

- `None`

## Files To Update

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`: Delete the commented-out highlight configuration block inside `configureQueryExecution(...)`.

## Library And Framework Decisions

- Reuse:
  - Existing `SolrQuery` execution setup: keep all active start/rows/sort/facet behavior intact.
- New dependency:
  - `None`
- Rejected options:
  - Converting the commented-out code into an explanatory comment: rejected because Sonar flags dead commented-out code, not missing explanation.
  - Combining this cleanup with other nearby Sonar fixes: rejected because each backlog item should remain independently implementable and traceable.

## Functional Requirements

- `FR-1`: Remove the inactive highlight-related comment lines in `configureQueryExecution(...)`.
- `FR-2`: Leave all active `solrQuery` calls before and after the removed block unchanged.
- `FR-3`: Preserve the runtime query behavior because the removed lines are currently inactive.

## API Contract

### Endpoint: `GET /api/search`

- Purpose: Preserve the existing search endpoint contract while removing dead commented-out code from the internal query-setup helper.
- Method: `GET`
- Path: `/api/search`
- Authentication: Unchanged.
- Query parameters: unchanged.
- Success response:
  - Status code: `200`
  - Content type: `application/json`
  - Response body: unchanged.
- Error responses:
  - Existing endpoint errors remain unchanged.
- Notes: This cleanup affects only an internal helper and should have no externally visible impact.

## Data Model And DB Schema

### Schema Changes

| Object | Change Type | Details |
| --- | --- | --- |
| `None` | `none` | `No schema, persistence, or migration work is part of this backlog item.` |

### SQL Or Migration Detail

```sql
-- None.
```

### Data Backfill And Rollback

- Backfill steps:
  - `None`
- Rollback expectations:
  - Revert only the deleted comment lines if the repository intentionally needs to preserve that inactive snippet for reference.
- Deployment ordering constraints:
  - `None`

## Phase Plan

### Phase 1: Remove the inactive highlight block

- Goal: Eliminate the Sonar-flagged commented-out code with no behavioral change.
- Prerequisites:
  - Confirm the highlighted lines are still commented out and not referenced elsewhere.
- Tasks:
  - `P1-T1`: Delete the commented-out `solrQuery.setHighlight(...)`, `hl.simple.*`, and `addHighlightField(...)` lines in `configureQueryExecution(...)`.
  - `P1-T2`: Keep the active sort and facet configuration lines unchanged.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`
- Acceptance criteria:
  - The inactive highlight block is removed completely.
  - The method still contains the same active query execution logic.

### Phase 2: Verify compile safety

- Goal: Confirm the cleanup does not affect compilation or active query setup.
- Prerequisites:
  - Phase 1 is complete.
- Tasks:
  - `P2-T1`: Review `configureQueryExecution(...)` to confirm no active statements were changed.
  - `P2-T2`: Run `mvn -pl backend -am -DskipTests compile` from the repo root to confirm the backend module still compiles after the cleanup.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`
- Acceptance criteria:
  - The backend module compiles successfully without unit tests.
  - No functional code outside the commented-out block changes.

## Risks And Operational Notes

- Risk:
  - Accidentally deleting nearby active query configuration while removing the comment block.
- Mitigation:
  - Limit the edit to the exact commented-out highlight lines flagged by Sonar.
- Monitoring or rollout note:
  - No rollout step is expected because the removed lines were inactive.

## Completion Criteria

- The listed files are fully implemented.
- The contract and schema changes match the spec.
- Each phase acceptance criterion is satisfied.
- No unresolved ambiguity remains for in-scope work.
