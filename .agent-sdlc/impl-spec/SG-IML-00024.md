---
id: "SG-IML-00024"
kind: implementation-spec
schema_version: 1
title: "Document intentional no-op 404 handler in V1OntologyTermController"
description: "Keep the local Spring Data REST 404 handler in V1OntologyTermController and add an explicit comment explaining the intentionally empty body."
status: draft
backlog_id: "SG-BLG-00009"
priority: p1
owner: "agent:Kestrel"
created_at: 2026-04-15T12:01:37Z
updated_at: 2026-04-15T12:01:37Z
module_scope:
  - "backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyTermController.java"
verification_mode: local_integration
tags: [springguard, sonarqube, controller, v1ontologytermcontroller, java-s1186]
links:
  requirements: []
  principles: []
  backlog: ["SG-BLG-00009"]
---

# Document intentional no-op 404 handler in V1OntologyTermController

## Summary

- Problem: `V1OntologyTermController.handleError` is intentionally empty, but Sonar `java:S1186` flags it because the reason is undocumented.
- Goal: Keep the current local 404 mapping and add a nested comment that explains why the handler body is intentionally empty.
- In scope:
  - Update only `handleError(HttpServletRequest req, Exception exception)` in `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyTermController.java`.
  - Preserve the existing `@ExceptionHandler(ResourceNotFoundException.class)` and `@ResponseStatus(HttpStatus.NOT_FOUND)` annotations.
  - Keep the change local to this controller.
- Out of scope:
  - Replacing the handler with a different advice path.
  - Refactoring unrelated controller logic.
  - Any route, response, or exception-type changes.

## Related Specs

- `.agent-sdlc/impl-spec/SG-IML-00023.md`

## Clarifications Required

- `CR-1`: None.
- `CR-2`: None.

## Existing Code Touchpoints

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyTermController.java`: Declares the empty local `@ExceptionHandler`.
- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/exception/GlobalExceptionHandler.java`: Handles a different exception class and should stay untouched.

## Files To Add

- `None`: No new files are required.

## Files To Update

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyTermController.java`: Add a concise comment to the empty handler and keep the annotations unchanged.

## Library And Framework Decisions

- Reuse:
  - Spring MVC `@ExceptionHandler` and `@ResponseStatus`: Preserve the current 404 mapping behavior.
  - `org.springframework.data.rest.webmvc.ResourceNotFoundException`: Keep the existing exception type handled by this controller.
- New dependency:
  - `None`.
- Rejected options:
  - Removing the handler and relying on `GlobalExceptionHandler`: That advice does not target the same exception class.
  - Throwing from the handler: Would change the current 404 behavior into a different runtime error path.

## Functional Requirements

- `FR-1`: The local `handleError` method must remain present and continue to translate Spring Data REST `ResourceNotFoundException` into `404 Not Found`.
- `FR-2`: The method body must contain a nested comment explaining that the method exists only to bind the response status.
- `FR-3`: No route mapping, status, reason text, or response payload behavior may change.

## API Contract

### Endpoint: Existing GET routes in V1OntologyTermController

- Purpose: Preserve the current 404 translation behavior for every route served by this controller.
- Method: `GET`
- Path:
  - `/api/ontologies/{onto}/terms`
  - `/api/ontologies/{onto}/terms/roots`
  - `/api/ontologies/{onto}/terms/preferredRoots`
  - `/api/ontologies/{onto}/terms/{iri}`
  - `/api/ontologies/{onto}/terms/{iri}/...`
  - `/api/ontologies/{onto}/children`
  - `/api/ontologies/{onto}/descendants`
  - `/api/ontologies/{onto}/hierarchicalChildren`
  - `/api/ontologies/{onto}/hierarchicalDescendants`
  - `/api/ontologies/{onto}/parents`
  - `/api/ontologies/{onto}/ancestors`
  - `/api/ontologies/{onto}/hierarchicalAncestors`
- Authentication:
  - Unchanged.
- Required headers:
  - `Accept`: Existing behavior remains unchanged.
- Path parameters:
  - Existing controller path parameters remain unchanged.
- Query parameters:
  - Existing controller query parameters remain unchanged.
- Request body:
  - Content type: `None`
  - Required: `false`
  - Fields:
    - `None`: No body contract change.
- Success response:
  - Status code: Existing success responses remain unchanged.
  - Content type: Existing controller behavior remains unchanged.
  - Fields:
    - `Unchanged`: This spec does not alter payload structure.
- Error responses:
  - `404`:
    - Trigger: The controller throws `org.springframework.data.rest.webmvc.ResourceNotFoundException`.
    - Response body:
      - `Unchanged`: The local handler behavior remains exactly as it is today.
- Idempotency or concurrency rules:
  - Unchanged.
- Notes:
  - This ticket documents the no-op handler body and does not change endpoint contracts.

## Data Model And DB Schema

### Schema Changes

| Object | Change Type | Details |
| --- | --- | --- |
| `none` | `none` | `No persistence changes.` |

### SQL Or Migration Detail

```sql
-- No schema change.
```

### Data Backfill And Rollback

- Backfill steps:
  - `None`
- Rollback expectations:
  - Remove the explanatory comment if the change is reverted.
- Deployment ordering constraints:
  - `None`

## Phase Plan

### Phase 1: Preserve the local 404 mapping

- Goal: Keep the current exception mapping and make the empty body intentional in code.
- Prerequisites:
  - Confirm the controller still handles Spring Data REST `ResourceNotFoundException`.
- Tasks:
  - `P1-T1`: Add a concise comment to `handleError`.
  - `P1-T2`: Leave annotations, signature, and exception type unchanged.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyTermController.java`
- Acceptance criteria:
  - The handler is no longer an undocumented empty block.
  - The controller still compiles as a no-op 404 handler.

### Phase 2: Verify narrow scope

- Goal: Ensure the remediation stays local and behavior-preserving.
- Prerequisites:
  - Phase 1 completed.
- Tasks:
  - `P2-T1`: Confirm no unrelated controller logic changed.
  - `P2-T2`: Keep imports untouched unless formatting cleanup is strictly required.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyTermController.java`
- Acceptance criteria:
  - The diff is limited to the handler body and any necessary formatting.
  - The controller still exposes the same routes and 404 behavior.

## Risks And Operational Notes

- Risk:
  - Replacing the local handler with another exception path would change runtime behavior across a large route surface.
- Mitigation:
  - Keep the local Spring Data REST handler and add only explanatory documentation.
- Monitoring or rollout note:
  - No config or rollout work is required.

## Completion Criteria

- The listed file is fully updated.
- The contract and schema sections remain unchanged in behavior.
- Each phase acceptance criterion is satisfied.
- No unresolved ambiguity remains for in-scope work.
