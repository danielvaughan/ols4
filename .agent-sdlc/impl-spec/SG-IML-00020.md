---
id: "SG-IML-00020"
kind: implementation-spec
schema_version: 1
title: "Document intentional no-op 404 handler in V1OntologyIndividualController"
description: "Keep the local Spring Data REST 404 handler in V1OntologyIndividualController and add an explicit comment explaining why the body is intentionally empty."
status: draft
backlog_id: "SG-BLG-00005"
priority: p1
owner: "agent:Kestrel"
created_at: 2026-04-15T12:01:37Z
updated_at: 2026-04-15T12:01:37Z
module_scope:
  - "backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyIndividualController.java"
verification_mode: local_integration
tags: [springguard, sonarqube, controller, v1ontologyindividualcontroller, java-s1186]
links:
  requirements: []
  principles: []
  backlog: ["SG-BLG-00005"]
---

# Document intentional no-op 404 handler in V1OntologyIndividualController

## Summary

- Problem: `V1OntologyIndividualController.handleError` is intentionally empty, but Sonar `java:S1186` flags the method because the reason is undocumented.
- Goal: Keep the local `@ExceptionHandler` behavior and add an explanatory nested comment that makes the no-op intentional and reviewable.
- In scope:
  - Update the empty `handleError(HttpServletRequest req, Exception exception)` method in `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyIndividualController.java`.
  - Preserve the existing `@ExceptionHandler(ResourceNotFoundException.class)` and `@ResponseStatus(HttpStatus.NOT_FOUND)` annotations.
  - Keep the change local to this controller.
- Out of scope:
  - Removing the handler.
  - Moving the behavior to a different controller or global advice.
  - Any changes to route mappings or response bodies.

## Related Specs

- `.agent-sdlc/impl-spec/SG-IML-00019.md`

## Clarifications Required

- `CR-1`: None.
- `CR-2`: None.

## Existing Code Touchpoints

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyIndividualController.java`: Declares the local `@ExceptionHandler` for Spring Data REST `ResourceNotFoundException`.
- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/exception/GlobalExceptionHandler.java`: Uses a different `ResourceNotFoundException` type and should remain unchanged for this ticket.

## Files To Add

- `None`: No new files are required.

## Files To Update

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyIndividualController.java`: Add an explanatory comment inside the no-op handler and keep annotations unchanged.

## Library And Framework Decisions

- Reuse:
  - Spring MVC `@ExceptionHandler` and `@ResponseStatus`: Preserve the current 404 mapping behavior.
  - `org.springframework.data.rest.webmvc.ResourceNotFoundException`: Keep the current exception type handled by this controller.
- New dependency:
  - `None`: No new libraries are needed.
- Rejected options:
  - Removing the handler in favor of `GlobalExceptionHandler`: That advice handles `uk.ac.ebi.spot.ols.controller.api.exception.ResourceNotFoundException`, not the Spring Data REST exception used here.
  - Throwing `UnsupportedOperationException`: Would change the route-level error behavior from 404 to 500.

## Functional Requirements

- `FR-1`: The local `handleError` method must remain present and continue to translate `org.springframework.data.rest.webmvc.ResourceNotFoundException` into `404 Not Found`.
- `FR-2`: The method body must contain a nested comment explaining why the handler intentionally performs no work.
- `FR-3`: The change must not alter endpoint mappings, status codes, reason text, or payload behavior for this controller.

## API Contract

### Endpoint: Existing GET routes in V1OntologyIndividualController

- Purpose: Preserve the current 404 translation behavior for all routes served by this controller.
- Method: `GET`
- Path:
  - `/api/ontologies/{onto}/individuals`
  - `/api/ontologies/{onto}/individuals/{iri}`
  - `/api/ontologies/{onto}/individuals/{iri}/types`
  - `/api/ontologies/{onto}/individuals/{iri}/alltypes`
  - `/api/ontologies/{onto}/individuals/{iri}/jstree`
- Authentication:
  - Unchanged.
- Required headers:
  - `Accept`: Existing behavior remains unchanged.
- Path parameters:
  - Existing controller path parameters remain unchanged.
- Query parameters:
  - Existing controller query parameters remain unchanged.
- Request body:
  - Content type: `None`
  - Required: `false`
  - Fields:
    - `None`: No body contract change.
- Success response:
  - Status code: Existing success responses remain unchanged.
  - Content type: Existing controller behavior remains unchanged.
  - Fields:
    - `Unchanged`: This spec does not alter payload structure.
- Error responses:
  - `404`:
    - Trigger: The controller throws `org.springframework.data.rest.webmvc.ResourceNotFoundException`.
    - Response body:
      - `Unchanged`: The local handler behavior remains exactly as it is today.
- Idempotency or concurrency rules:
  - Unchanged.
- Notes:
  - The handler body stays empty by design; only the rationale becomes explicit in code.

## Data Model And DB Schema

### Schema Changes

| Object | Change Type | Details |
| --- | --- | --- |
| `none` | `none` | `No persistence changes.` |

### SQL Or Migration Detail

```sql
-- No schema change.
```

### Data Backfill And Rollback

- Backfill steps:
  - `None`
- Rollback expectations:
  - Remove the explanatory comment if the change must be reverted.
- Deployment ordering constraints:
  - `None`

## Phase Plan

### Phase 1: Preserve the handler contract

- Goal: Keep the local 404 handler in place and make its intent explicit.
- Prerequisites:
  - Confirm the controller continues to use Spring Data REST `ResourceNotFoundException`.
- Tasks:
  - `P1-T1`: Add a concise comment inside `handleError` explaining that the method exists only to bind `@ResponseStatus` to the exception handler.
  - `P1-T2`: Leave method signature, annotations, and exception type unchanged.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyIndividualController.java`
- Acceptance criteria:
  - The method is no longer an undocumented empty block.
  - The handler still compiles as a no-op 404 mapping.

### Phase 2: Verify narrow scope

- Goal: Keep the remediation local and behavior-preserving.
- Prerequisites:
  - Phase 1 completed.
- Tasks:
  - `P2-T1`: Confirm no unrelated logic or route changes were introduced.
  - `P2-T2`: Keep imports untouched unless cleanup is strictly required by the chosen comment form.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyIndividualController.java`
- Acceptance criteria:
  - The diff is limited to the handler body and any necessary formatting.
  - The controller still advertises the same route and error behavior.

## Risks And Operational Notes

- Risk:
  - Replacing the local handler with broader exception-advice logic would subtly change which exception class is caught.
- Mitigation:
  - Keep the local handler and explain its purpose instead of refactoring the error path.
- Monitoring or rollout note:
  - No runtime configuration or rollout steps are required.

## Completion Criteria

- The listed file is fully updated.
- The contract and schema sections remain unchanged in behavior.
- Each phase acceptance criterion is satisfied.
- No unresolved ambiguity remains for in-scope work.
