---
id: "SG-IML-00050"
kind: implementation-spec
schema_version: 1
title: "Resolve stale TODO in configureQueryExecution"
description: "Plan a controller-local cleanup that resolves Sonar rule java:S1135 by removing the stale `TODO` comment above the active facet-field configuration in `V1SearchController.configureQueryExecution(...)`."
status: draft
backlog_id: "SG-BLG-00041"
priority: p1
owner: "agent:Rook"
created_at: 2026-04-15T15:39:44Z
updated_at: 2026-04-15T15:39:44Z
module_scope:
  - "backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java"
verification_mode: manual
tags:
  - springguard
  - sonarqube
  - controller
  - v1searchcontroller
  - java-s1135
links:
  requirements: []
  principles: []
  backlog: ["SG-BLG-00041"]
---

# V1SearchController Implementation Spec

## Summary

- Problem: `configureQueryExecution(...)` contains a stale `TODO` comment about adding additional faceted fields, and Sonar flags it under `java:S1135`.
- Goal: Remove the unresolved TODO text while keeping the active facet-field configuration and adjacent historical comment block unchanged unless the implementation confirms the TODO has already been satisfied.
- In scope: Resolve the flagged TODO comment at the current line by deleting it.
- Out of scope: Expanding the facet-field list, redesigning facet handling, or rewriting the surrounding historical issue comment block.

## Related Specs

- `SG-IML-00048`: Adjacent removal of the commented-out highlight block in the same helper.
- `SG-IML-00049`: Adjacent removal of the commented-out `addFacetField(...)` line in the same helper.

## Clarifications Required

- `None`

## Existing Code Touchpoints

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`: `configureQueryExecution(...)` contains the flagged TODO comment directly above the active `solrQuery.addFacetField(...)` call.
- Active facet-field list below the TODO: must remain unchanged for this backlog item.

## Files To Add

- `None`

## Files To Update

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`: Remove the stale `TODO` comment above the active facet-field configuration in `configureQueryExecution(...)`.

## Library And Framework Decisions

- Reuse:
  - Existing active Solr facet-field configuration: keep the current field list unchanged.
  - Existing historical issue block comment: preserve it unless implementation proves it is also stale, which is outside this backlog item.
- New dependency:
  - `None`
- Rejected options:
  - Replacing the TODO with another open-ended note: rejected because it would preserve the underlying Sonar concern.
  - Adding new facet fields without evidence from current requirements: rejected because it would widen scope and risk changing search behavior.

## Functional Requirements

- `FR-1`: Remove the flagged `TODO` comment from `configureQueryExecution(...)`.
- `FR-2`: Preserve the active `solrQuery.addFacetField(...)` invocation and its current field list.
- `FR-3`: Keep endpoint behavior unchanged because the change affects only stale commentary.

## API Contract

### Endpoint: `GET /api/search`

- Purpose: Preserve the existing search endpoint contract while removing stale TODO text from the internal query-setup helper.
- Method: `GET`
- Path: `/api/search`
- Authentication: Unchanged.
- Query parameters: unchanged.
- Success response:
  - Status code: `200`
  - Content type: `application/json`
  - Response body: unchanged.
- Error responses:
  - Existing endpoint errors remain unchanged.
- Notes: This cleanup is internal only and should not affect request handling or response content.

## Data Model And DB Schema

### Schema Changes

| Object | Change Type | Details |
| --- | --- | --- |
| `None` | `none` | `No schema, persistence, or migration work is part of this backlog item.` |

### SQL Or Migration Detail

```sql
-- None.
```

### Data Backfill And Rollback

- Backfill steps:
  - `None`
- Rollback expectations:
  - Revert only the removed TODO comment if repository history needs that note retained for manual tracking.
- Deployment ordering constraints:
  - `None`

## Phase Plan

### Phase 1: Remove the stale TODO comment

- Goal: Eliminate the Sonar-flagged TODO without changing active facet behavior.
- Prerequisites:
  - Confirm the active facet-field list below the comment already represents the intended current state.
- Tasks:
  - `P1-T1`: Delete the `// TODO: Need to check and add additional faceted fields if required` line.
  - `P1-T2`: Leave the active `solrQuery.addFacetField(...)` block unchanged.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`
- Acceptance criteria:
  - The TODO comment is removed completely.
  - The active facet configuration remains unchanged.

### Phase 2: Verify compile safety

- Goal: Confirm the cleanup is behavior-neutral and compile-safe.
- Prerequisites:
  - Phase 1 is complete.
- Tasks:
  - `P2-T1`: Review `configureQueryExecution(...)` and confirm only the TODO comment changed.
  - `P2-T2`: Run `mvn -pl backend -am -DskipTests compile` from the repo root to confirm the backend module still compiles after the cleanup.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`
- Acceptance criteria:
  - The backend module compiles successfully without unit tests.
  - No active query or facet behavior changes.

## Risks And Operational Notes

- Risk:
  - Misreading the TODO as a requirement to add new facet fields during this cleanup.
- Mitigation:
  - Constrain the implementation to removing stale commentary only and preserve the current active field list.
- Monitoring or rollout note:
  - No rollout step is expected because the runtime behavior does not change.

## Completion Criteria

- The listed files are fully implemented.
- The contract and schema changes match the spec.
- Each phase acceptance criterion is satisfied.
- No unresolved ambiguity remains for in-scope work.
