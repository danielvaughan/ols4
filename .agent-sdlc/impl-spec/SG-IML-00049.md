---
id: "SG-IML-00049"
kind: implementation-spec
schema_version: 1
title: "Remove commented-out facet-field line from configureQueryExecution"
description: "Plan a controller-local cleanup that resolves Sonar rule java:S125 by deleting the commented-out `solrQuery.addFacetField(...)` line in `V1SearchController.configureQueryExecution(...)`."
status: draft
backlog_id: "SG-BLG-00040"
priority: p1
owner: "agent:Rook"
created_at: 2026-04-15T15:39:00Z
updated_at: 2026-04-15T15:39:00Z
module_scope:
  - "backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java"
verification_mode: manual
tags:
  - springguard
  - sonarqube
  - controller
  - v1searchcontroller
  - java-s125
links:
  requirements: []
  principles: []
  backlog: ["SG-BLG-00040"]
---

# V1SearchController Implementation Spec

## Summary

- Problem: `configureQueryExecution(...)` still contains a commented-out `solrQuery.addFacetField(...)` line that Sonar flags as dead code under `java:S125`.
- Goal: Remove that inactive `addFacetField(...)` line without changing the active facet configuration that follows it.
- In scope: Delete only the flagged commented-out facet-field line.
- Out of scope: Changing the active facet-field list, removing nearby prose comments, or revisiting highlight behavior.

## Related Specs

- `SG-IML-00048`: Adjacent removal of the commented-out highlight block in the same method.
- `SG-IML-00050`: Adjacent cleanup of the remaining TODO comment in the same method.

## Clarifications Required

- `None`

## Existing Code Touchpoints

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`: `configureQueryExecution(...)` contains the flagged commented-out `addFacetField(...)` line immediately above the active facet configuration block.
- Active `solrQuery.addFacetField(...)` call below the comment: must remain unchanged.

## Files To Add

- `None`

## Files To Update

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`: Delete the single commented-out `addFacetField(...)` line inside `configureQueryExecution(...)`.

## Library And Framework Decisions

- Reuse:
  - Existing active Solr facet configuration below the comment: keep it intact.
- New dependency:
  - `None`
- Rejected options:
  - Replacing the commented-out line with a prose note: rejected because the issue is inactive code, not missing explanation.
  - Folding this cleanup into a larger `configureQueryExecution(...)` refactor: rejected because the backlog item is scoped to one flagged line.

## Functional Requirements

- `FR-1`: Remove the commented-out `solrQuery.addFacetField(...)` line in `configureQueryExecution(...)`.
- `FR-2`: Preserve the active facet-field configuration block that follows the removed line.
- `FR-3`: Keep runtime search behavior unchanged because the removed line is currently inactive.

## API Contract

### Endpoint: `GET /api/search`

- Purpose: Preserve the existing search endpoint contract while removing dead commented-out code from the internal Solr-query setup helper.
- Method: `GET`
- Path: `/api/search`
- Authentication: Unchanged.
- Query parameters: unchanged.
- Success response:
  - Status code: `200`
  - Content type: `application/json`
  - Response body: unchanged.
- Error responses:
  - Existing endpoint errors remain unchanged.
- Notes: The cleanup is internal only and should not alter response payloads or filtering behavior.

## Data Model And DB Schema

### Schema Changes

| Object | Change Type | Details |
| --- | --- | --- |
| `None` | `none` | `No schema, persistence, or migration work is part of this backlog item.` |

### SQL Or Migration Detail

```sql
-- None.
```

### Data Backfill And Rollback

- Backfill steps:
  - `None`
- Rollback expectations:
  - Revert only the deleted comment line if the repository intentionally needs to preserve that inactive snippet for reference.
- Deployment ordering constraints:
  - `None`

## Phase Plan

### Phase 1: Remove the inactive facet-field line

- Goal: Eliminate the Sonar-flagged commented-out line with no functional change.
- Prerequisites:
  - Confirm the flagged `addFacetField(...)` statement is still commented out.
- Tasks:
  - `P1-T1`: Delete the commented-out `solrQuery.addFacetField("ontology_name", "ontology_prefix", "type", "subset", "is_defining_ontology", "is_obsolete");` line.
  - `P1-T2`: Leave the active facet-field setup below it unchanged.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`
- Acceptance criteria:
  - The inactive `addFacetField(...)` line is removed completely.
  - The active facet configuration remains intact.

### Phase 2: Verify compile safety

- Goal: Confirm the cleanup is behavior-neutral and compile-safe.
- Prerequisites:
  - Phase 1 is complete.
- Tasks:
  - `P2-T1`: Review `configureQueryExecution(...)` and confirm only the commented-out line changed.
  - `P2-T2`: Run `mvn -pl backend -am -DskipTests compile` from the repo root to confirm the backend module still compiles after the cleanup.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`
- Acceptance criteria:
  - The backend module compiles successfully without unit tests.
  - No active query setup behavior changes.

## Risks And Operational Notes

- Risk:
  - Deleting nearby active facet configuration instead of only the commented-out line.
- Mitigation:
  - Keep the edit narrowly scoped to the exact line flagged by Sonar.
- Monitoring or rollout note:
  - No rollout step is expected because the removed code was inactive.

## Completion Criteria

- The listed files are fully implemented.
- The contract and schema changes match the spec.
- Each phase acceptance criterion is satisfied.
- No unresolved ambiguity remains for in-scope work.
