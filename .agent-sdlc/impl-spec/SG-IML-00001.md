---
id: "SG-IML-00001"
kind: implementation-spec
schema_version: 1
title: "Document the intentional no-op not-found handler in V1IndividualController"
description: "Clarify why V1IndividualController keeps an empty ResourceNotFoundException handler so the controller preserves its current 404 behavior while satisfying the empty-method finding."
status: draft
backlog_id: "SG-BLG-00002"
priority: p1
owner: agent:Rook
created_at: 2026-04-15T11:57:05Z
updated_at: 2026-04-15T11:57:05Z
module_scope:
  - backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1IndividualController.java
verification_mode: local_integration
tags:
  - springguard
  - implementation-spec
  - controller
  - v1individualcontroller
links:
  requirements: []
  principles: []
  backlog:
    - "SG-BLG-00002"
---

# V1IndividualController Not-Found Handler Implementation Spec

## Summary

- Problem: `V1IndividualController.handleError(HttpServletRequest, Exception)` is intentionally empty, but Sonar flags it because the method body contains no explanation.
- Goal: Keep the current `404 Not Found` mapping behavior and make the empty body self-explanatory to maintainers and static analysis.
- In scope: The annotated exception-handler method in `V1IndividualController` only.
- Out of scope: Repository behavior, response payload changes, logging policy changes, or changes to sibling controllers.

## Related Specs

- `None.`

## Clarifications Required

- `CR-1`: `None.`
- `CR-2`: `None.`

## Existing Code Touchpoints

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1IndividualController.java`: owns the `@ExceptionHandler(ResourceNotFoundException.class)` method that currently has an empty body.

## Files To Add

- `None.`

## Files To Update

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1IndividualController.java`: add an inline explanation inside the empty handler body and keep the current annotation-driven behavior unchanged.

## Library And Framework Decisions

- Reuse:
  - `Spring MVC exception handling annotations`: the current `@ResponseStatus` plus `@ExceptionHandler` pattern already performs the required 404 translation.
- New dependency:
  - `None`: the fix is documentation-only inside an existing controller method.
- Rejected options:
  - `Throwing UnsupportedOperationException`: would change a handled 404 path into a different failure mode.
  - `Adding response writes or logging side effects`: would widen scope beyond the Sonar finding and risk changing API behavior.

## Functional Requirements

- `FR-1`: Leave the annotated exception-handler method in place so `ResourceNotFoundException` still maps to `404 Not Found`.
- `FR-2`: Add a short nested comment inside the method body stating that no body logic is required because Spring handles the status via annotations.
- `FR-3`: Do not change method visibility, endpoint mappings, response content, or exception propagation as part of this ticket.

## API Contract

### Endpoint: `V1IndividualController GET endpoints`

- Purpose: Preserve current 404 translation for `ResourceNotFoundException` thrown while serving `/api/individuals` requests.
- Method: `GET`
- Path: `/api/individuals/**`
- Authentication: no new authentication behavior may be introduced by this ticket.
- Required headers:
  - `Accept`: unchanged.
- Path parameters:
  - `Unchanged.`
- Query parameters:
  - `Unchanged.`
- Request body:
  - Content type: `None`
  - Required: `No`
  - Fields:
    - `None.`
- Success response:
  - Status code: `200`
  - Content type: `application/json` or `application/hal+json`
  - Fields:
    - `Unchanged by this ticket.`
- Error responses:
  - `404`:
    - Trigger: `ResourceNotFoundException` is raised inside the controller flow.
    - Response body:
      - `Unchanged; this ticket must not introduce a new error body.`
- Idempotency or concurrency rules: unchanged.
- Notes: This is a comment-only clarification ticket; no runtime contract change is allowed.

## Data Model And DB Schema

### Schema Changes

| Object | Change Type | Details |
| --- | --- | --- |
| `Database schema` | `none` | `No schema or persistence changes are involved.` |

### SQL Or Migration Detail

```sql
-- No schema changes.
```

### Data Backfill And Rollback

- Backfill steps:
  - `None.`
- Rollback expectations:
  - `Remove the explanatory comment if the handler implementation is later replaced.`
- Deployment ordering constraints:
  - `None.`

## Phase Plan

### Phase 1: Clarify the empty handler

- Goal: Make the no-op handler intentional and reviewable.
- Prerequisites: `None.`
- Tasks:
  - `P1-T1`: Add a short nested comment inside `handleError` that explains the annotation-driven 404 mapping.
  - `P1-T2`: Verify that the method body remains side-effect free after the comment is added.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1IndividualController.java`
- Acceptance criteria:
  - `The empty-body Sonar finding is addressed by an explicit in-method explanation.`
  - `The handler still performs no runtime work beyond Spring's existing annotation handling.`

## Risks And Operational Notes

- Risk:
  - `A larger "fix" than a comment could accidentally change 404 behavior for existing clients.`
- Mitigation:
  - `Limit the change to an explanatory comment and preserve the current annotations and method signature.`
- Monitoring or rollout note:
  - `No rollout coordination is required because there is no intended behavior change.`

## Completion Criteria

- The listed files are fully implemented.
- The contract and schema changes match the spec.
- Each phase acceptance criterion is satisfied.
- No unresolved ambiguity remains for in-scope work.
