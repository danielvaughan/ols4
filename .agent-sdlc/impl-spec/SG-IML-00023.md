---
id: "SG-IML-00023"
kind: implementation-spec
schema_version: 1
title: "Extract duplicated \"defType\" constant in V1SearchController"
description: "Plan a controller-local refactor that replaces the duplicated `defType` literal in V1SearchController with one class-level constant while preserving existing request and response behavior."
status: draft
backlog_id: "SG-BLG-00013"
priority: p1
owner: "agent:Rook"
created_at: 2026-04-15T14:11:33Z
updated_at: 2026-04-15T14:11:33Z
module_scope:
  - "backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java"
verification_mode: manual
tags:
  - springguard
  - sonarqube
  - controller
  - v1searchcontroller
  - java-s1192
links:
  requirements: []
  principles: []
  backlog: ["SG-BLG-00013"]
---

# V1SearchController Implementation Spec

## Summary

- Problem: `V1SearchController` repeats the exact `defType` literal in `3` places inside the Solr query parser setup inside `search(...)`, which triggers Sonar rule `java:S1192`.
- Goal: Replace those duplicated literal uses with a single controller-scoped constant named `DEF_TYPE_PARAM` without changing the public contract or internal runtime semantics.
- In scope: Add `DEF_TYPE_PARAM` as a private class-level constant and replace the exact standalone `defType` occurrences described by Sonar in `V1SearchController`.
- Out of scope: changing request mappings, renaming public fields or parameters, or merging this change with unrelated refactors, changing the `"edismax"` literal (tracked separately by `SG-BLG-00014`) or the broader complexity refactor tracked by `SG-BLG-00009`.

## Related Specs

- `None`

## Clarifications Required

- `None`

## Existing Code Touchpoints

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`: Contains the repeated `solrQuery.set(...)` calls that configure the query parser.
- Existing `SHORT_FORM_FIELD` constant in `V1SearchController`: keep the current in-progress constant extraction intact and add the new constant alongside it rather than reworking unrelated lines.

## Files To Add

- `None`

## Files To Update

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`: Introduce `DEF_TYPE_PARAM` and replace the exact standalone `defType` usages in the in-scope branch(es).

## Library And Framework Decisions

- Reuse:
  - Controller-local `private static final String` constant: smallest maintainable fix for Sonar `java:S1192` in a single controller.
  - Existing controller flow and public field names: must stay unchanged after the constant extraction.
- New dependency:
  - `None`: no new library is required for a local literal extraction.
- Rejected options:
  - Renaming the public field or request parameter `defType`: rejected because the backlog item is only about internal literal deduplication, not contract changes.
  - Folding several neighboring literal extractions into one broad refactor: rejected because each backlog item must remain traceable and independently implementable.
  - Reusing a shared cross-controller constants class: rejected because the requested fix is intentionally controller-local.

## Functional Requirements

- `FR-1`: `V1SearchController` shall declare exactly one private class-level constant named `DEF_TYPE_PARAM` whose value is exactly `defType`.
- `FR-2`: Use `DEF_TYPE_PARAM` in the exact `SolrQuery` configuration and response-mapping branches that currently repeat `defType`.
- `FR-3`: No exact standalone `defType` literal flagged by this backlog item shall remain in the targeted branch(es) after the refactor.
- `FR-4`: Request mappings, response body shape, Solr parameter semantics, and repository/service interactions shall remain unchanged.
- `FR-5`: If `DEF_TYPE_PARAM` or an equivalent constant already exists in the controller by the time implementation begins, the implementation shall reuse that existing constant instead of creating a duplicate.

## API Contract

### Endpoint: `/api/search`

- Purpose: Preserve the current `/api/search` request and response contract while removing the duplicated internal Solr literal `defType`.
- Method: `GET`
- Path: `/api/search`
- Authentication: Unchanged.
- Query parameters:
  - Existing request parameters remain unchanged; this backlog item only changes how the controller stores one repeated internal constant.
- Success response:
  - Status code: `200`
  - Content type: `application/json`
  - Response fields: unchanged.
- Error responses:
  - Existing endpoint errors remain unchanged.
- Notes: `SolrQuery` configuration must keep using the same parameter names, values, field ordering, and sort behavior after the constant extraction.
## Data Model And DB Schema

### Schema Changes

| Object | Change Type | Details |
| --- | --- | --- |
| `None` | `none` | `No schema, persistence, or migration changes are part of this backlog item.` |

### SQL Or Migration Detail

```sql
-- None.
```

### Data Backfill And Rollback

- Backfill steps:
  - `None`
- Rollback expectations:
  - Revert only the constant extraction if manual verification shows a behavioral regression.
- Deployment ordering constraints:
  - `None`

## Phase Plan

### Phase 1: Introduce the controller-local constant

- Goal: Centralize `defType` in one named constant.
- Prerequisites:
  - Confirm the backlog item still maps to the exact standalone `defType` uses described by Sonar.
- Tasks:
  - `P1-T1`: Add `private static final String DEF_TYPE_PARAM = "defType";` near the existing class-level constants.
  - `P1-T2`: Replace the exact standalone `defType` occurrences covered by this backlog item with `DEF_TYPE_PARAM`.
  - `P1-T3`: Preserve all unrelated literal extractions and in-progress work already present in the file.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`
- Acceptance criteria:
  - `V1SearchController` contains a single authoritative `DEF_TYPE_PARAM` constant for `defType`.
  - The in-scope branch(es) no longer repeat the standalone `defType` literal.

### Phase 2: Verify behavior remains unchanged

- Goal: Confirm the constant extraction did not alter endpoint or query behavior.
- Prerequisites:
  - Phase 1 is complete.
- Tasks:
  - `P2-T1`: Review the affected branch(es) and confirm all call sites still use the same response key, request parameter, or Solr parameter/value as before.
  - `P2-T2`: Run `mvn -pl backend -am -DskipTests compile` from the repo root to confirm the backend module still compiles after the change.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`
- Acceptance criteria:
  - The backend module compiles successfully without enabling unit tests.
  - Public behavior and internal query wiring remain unchanged aside from constant reuse.

## Risks And Operational Notes

- Risk:
  - A broad cleanup in the same method could accidentally absorb unrelated backlog items or disturb current worktree changes.
- Mitigation:
  - Limit the implementation to `DEF_TYPE_PARAM` and the exact standalone `defType` occurrences covered by this backlog item.
- Monitoring or rollout note:
  - After implementation, manually exercise the affected endpoint and confirm the same response keys and filter behavior are still present.

## Completion Criteria

- The listed files are fully implemented.
- The contract and schema changes match the spec.
- Each phase acceptance criterion is satisfied.
- No unresolved ambiguity remains for in-scope work.
