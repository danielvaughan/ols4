---
id: "SG-IML-00055"
kind: implementation-spec
schema_version: 1
title: "Replace addQuotes anonymous class with lambda"
description: "Plan a controller-local cleanup that resolves Sonar rule java:S1604 by replacing the anonymous inner class assigned to `addQuotes` in `V1SearchController` with a lambda expression."
status: draft
backlog_id: "SG-BLG-00046"
priority: p1
owner: "agent:Rook"
created_at: 2026-04-15T15:43:04Z
updated_at: 2026-04-15T15:43:04Z
module_scope:
  - "backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java"
verification_mode: manual
tags:
  - springguard
  - sonarqube
  - controller
  - v1searchcontroller
  - java-s1604
links:
  requirements: []
  principles: []
  backlog: ["SG-BLG-00046"]
---

# V1SearchController Implementation Spec

## Summary

- Problem: `V1SearchController` declares `addQuotes` with an anonymous inner class implementing a functional interface, and Sonar flags that pattern under `java:S1604`.
- Goal: Replace the anonymous inner class with an equivalent lambda expression while preserving the exact string-quoting behavior.
- In scope: Update only the `addQuotes` declaration and keep its current call sites and quoting behavior unchanged.
- Out of scope: Changing the quoting logic, altering call-site behavior, or widening the change beyond this one functional declaration.

## Related Specs

- `SG-IML-00056`: Tightens the same `addQuotes` declaration to `UnaryOperator<String>`; both specs should converge on a single lambda-based declaration.

## Clarifications Required

- `None`

## Existing Code Touchpoints

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`: Contains the current `addQuotes` declaration as an anonymous inner class with a single `apply(...)` method.
- Existing `createUnionQuery(...)` logic: depends on `addQuotes` producing the same quoted string values as today.
> [ ] [Kestrel (Codex: 019d915e-24a7-7ef1-81fe-9a282f24cc41)] [2026-04-15T15:52:15Z]: This touchpoint is source-incorrect. In the current controller, `createUnionQuery(...)` never calls `addQuotes`; it builds its own quoted segments inline. The real call chain is `joinQuotedValues(...)` -> `addChildrenFilter(...)` / `addAllChildrenFilter(...)` in `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`. Please replace this reference with the actual `addQuotes` call sites so a junior engineer validates the right behavior after the lambda conversion.

## Files To Add

- `None`

## Files To Update

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`: Replace the anonymous inner class assigned to `addQuotes` with a lambda expression.

## Library And Framework Decisions

- Reuse:
  - Java lambda syntax as the direct replacement for a single-method anonymous functional implementation.
  - Existing string-building logic inside `apply(...)`: preserve it exactly inside the lambda body.
- New dependency:
  - `None`
- Rejected options:
  - Rewriting the quoting helper as a named method: rejected because the Sonar issue is specifically about the anonymous functional implementation.
  - Simplifying the quoting algorithm at the same time: rejected because the backlog item is about the declaration style, not the output.

## Functional Requirements

- `FR-1`: Replace the anonymous inner class used for `addQuotes` with a lambda expression.
- `FR-2`: Preserve the same quoted output for every input string.
- `FR-3`: Keep existing call sites and control flow unchanged.
- `FR-4`: Allow the final declaration to adopt `UnaryOperator<String>` if implemented together with the related interface-tightening backlog item.

## API Contract

### Endpoint: `GET /api/search`

- Purpose: Preserve the existing search endpoint contract while modernizing one internal functional helper declaration.
- Method: `GET`
- Path: `/api/search`
- Authentication: Unchanged.
- Query parameters: unchanged.
- Success response:
  - Status code: `200`
  - Content type: `application/json`
  - Response body: unchanged.
- Error responses:
  - Existing endpoint errors remain unchanged.
- Notes: This is an internal declaration cleanup and should not affect search results.

## Data Model And DB Schema

### Schema Changes

| Object | Change Type | Details |
| --- | --- | --- |
| `None` | `none` | `No schema, persistence, or migration work is part of this backlog item.` |

### SQL Or Migration Detail

```sql
-- None.
```

### Data Backfill And Rollback

- Backfill steps:
  - `None`
- Rollback expectations:
  - Revert only the declaration-style change if it introduces an unexpected syntax or typing issue.
- Deployment ordering constraints:
  - `None`

## Phase Plan

### Phase 1: Convert the anonymous class to a lambda

- Goal: Make the helper declaration Sonar-compliant without changing behavior.
- Prerequisites:
  - Confirm the anonymous inner class is only used as a functional helper and has no side effects beyond returning the quoted string.
- Tasks:
  - `P1-T1`: Replace the anonymous inner class body with an equivalent lambda expression.
  - `P1-T2`: Keep the existing quoted-string construction logic intact.
  - `P1-T3`: If the related interface-tightening item is implemented at the same time, ensure both changes land in a single `UnaryOperator<String> addQuotes = s -> ...;` style declaration.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`
- Acceptance criteria:
  - The anonymous inner class is removed.
  - `addQuotes` becomes a lambda-based declaration with unchanged behavior.

### Phase 2: Verify compile safety

- Goal: Confirm the declaration cleanup compiles and preserves quoting behavior.
- Prerequisites:
  - Phase 1 is complete.
- Tasks:
  - `P2-T1`: Review the updated declaration and confirm the output logic matches the old `apply(...)` implementation.
  - `P2-T2`: Run `mvn -pl backend -am -DskipTests compile` from the repo root to confirm the backend module still compiles after the cleanup.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1SearchController.java`
- Acceptance criteria:
  - The backend module compiles successfully without unit tests.
  - Quoted-string construction remains unchanged.

## Risks And Operational Notes

- Risk:
  - Accidentally simplifying the string-building logic while converting to lambda syntax.
- Mitigation:
  - Preserve the existing output logic exactly and limit the change to declaration style.
- Monitoring or rollout note:
  - No rollout step is expected because the helper behavior is unchanged.

## Completion Criteria

- The listed files are fully implemented.
- The contract and schema changes match the spec.
- Each phase acceptance criterion is satisfied.
- No unresolved ambiguity remains for in-scope work.
