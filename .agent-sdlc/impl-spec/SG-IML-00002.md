---
id: "SG-IML-00002"
kind: implementation-spec
schema_version: 1
title: "Document the intentional no-op not-found handler in V1OntologyController"
description: "Clarify why V1OntologyController keeps an empty ResourceNotFoundException handler so the controller preserves its current 404 behavior while satisfying the empty-method finding."
status: draft
backlog_id: "SG-BLG-00003"
priority: p1
owner: agent:Rook
created_at: 2026-04-15T11:57:05Z
updated_at: 2026-04-15T11:57:05Z
module_scope:
  - backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyController.java
verification_mode: local_integration
tags:
  - springguard
  - implementation-spec
  - controller
  - v1ontologycontroller
links:
  requirements: []
  principles: []
  backlog:
    - "SG-BLG-00003"
---

# V1OntologyController Not-Found Handler Implementation Spec

## Summary

- Problem: `V1OntologyController.handleError(HttpServletRequest, Exception)` is intentionally empty, but Sonar flags it because the body does not explain the annotation-driven behavior.
- Goal: Keep the existing `404 Not Found` mapping semantics and make the no-op method explicit to future maintainers.
- In scope: The `@ExceptionHandler(ResourceNotFoundException.class)` method in `V1OntologyController`.
- Out of scope: Ontology lookup logic, HATEOAS assembly, response body changes, or logging policy changes.

## Related Specs

- `None.`

## Clarifications Required

- `CR-1`: `None.`
- `CR-2`: `None.`

## Existing Code Touchpoints

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyController.java`: defines the controller-level `ResourceNotFoundException` handler that currently has an empty body.

## Files To Add

- `None.`

## Files To Update

- `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyController.java`: add an explanatory comment inside the empty handler and preserve all current annotation behavior.

## Library And Framework Decisions

- Reuse:
  - `Spring MVC exception handling annotations`: the current annotations already supply the desired HTTP status mapping without additional code.
- New dependency:
  - `None`: this ticket is limited to a controller-local clarification.
- Rejected options:
  - `Replacing the handler with a thrown exception`: would change the endpoint error behavior.
  - `Adding response-building logic`: is unnecessary because the controller already relies on the annotations for status handling.

## Functional Requirements

- `FR-1`: Preserve the existing `@ResponseStatus(HttpStatus.NOT_FOUND)` plus `@ExceptionHandler(ResourceNotFoundException.class)` behavior.
- `FR-2`: Add an in-method comment explaining that the method is intentionally empty because Spring handles the response status.
- `FR-3`: Do not add side effects such as logging, response writes, or custom error payload generation.

## API Contract

### Endpoint: `V1OntologyController GET endpoints`

- Purpose: Preserve current 404 translation for ontology lookups served from `/api/ontologies`.
- Method: `GET`
- Path: `/api/ontologies/**`
- Authentication: unchanged.
- Required headers:
  - `Accept`: unchanged.
- Path parameters:
  - `Unchanged.`
- Query parameters:
  - `Unchanged.`
- Request body:
  - Content type: `None`
  - Required: `No`
  - Fields:
    - `None.`
- Success response:
  - Status code: `200`
  - Content type: `application/json` or `application/hal+json`
  - Fields:
    - `Unchanged by this ticket.`
- Error responses:
  - `404`:
    - Trigger: `ResourceNotFoundException` is raised during ontology retrieval.
    - Response body:
      - `Unchanged; this ticket must not introduce a new body contract.`
- Idempotency or concurrency rules: unchanged.
- Notes: The implementation must remain behaviorally identical to the current controller.

## Data Model And DB Schema

### Schema Changes

| Object | Change Type | Details |
| --- | --- | --- |
| `Database schema` | `none` | `No schema or migration work is needed.` |

### SQL Or Migration Detail

```sql
-- No schema changes.
```

### Data Backfill And Rollback

- Backfill steps:
  - `None.`
- Rollback expectations:
  - `Remove the explanatory comment if a later ticket replaces the no-op handler with real logic.`
- Deployment ordering constraints:
  - `None.`

## Phase Plan

### Phase 1: Clarify the empty handler

- Goal: Make the no-op handler explicitly intentional.
- Prerequisites: `None.`
- Tasks:
  - `P1-T1`: Add a short nested comment inside `handleError` that documents the annotation-driven 404 handling.
  - `P1-T2`: Keep the method body free of runtime side effects after the comment is added.
- Files touched:
  - `backend/src/main/java/uk/ac/ebi/spot/ols/controller/api/v1/V1OntologyController.java`
- Acceptance criteria:
  - `The empty handler contains a clear explanation for why no executable code is needed.`
  - `Endpoint success and 404 behavior stay unchanged.`

## Risks And Operational Notes

- Risk:
  - `Changing the empty handler beyond a comment could alter 404 handling for existing ontology clients.`
- Mitigation:
  - `Restrict the ticket to an explanatory comment and leave annotations and signatures intact.`
- Monitoring or rollout note:
  - `No rollout coordination is required for a comment-only clarification.`

## Completion Criteria

- The listed files are fully implemented.
- The contract and schema changes match the spec.
- Each phase acceptance criterion is satisfied.
- No unresolved ambiguity remains for in-scope work.
